import { xstateMatchingExample } from './xstate-matching-example';

describe('xstateMatchingExample', () => {
  it('should work', () => {
    expect(xstateMatchingExample()).toEqual('a1!');
  });
});
